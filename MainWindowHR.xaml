<Window x:Class="DoAn.MainWindowHR"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QUẢN LÝ NHÂN SỰ"
        WindowStyle="None" ResizeMode="NoResize"
        Height="650" Width="1100">

    <Window.Resources>
        <Style x:Key="HRMenuBtn" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="BdHR" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BdHR" Property="Background" Value="#D1E8FF"/>
                                <Setter Property="Foreground" Value="#1A5276"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="White" BorderBrush="#DDD" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <StackPanel HorizontalAlignment="Center" Margin="0,20,0,30">
                    <Ellipse Width="70" Height="70" Fill="#EEE"/>
                    <TextBlock x:Name="txtHRUserName" Text="HR Admin" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>

                <StackPanel>
                    <ToggleButton x:Name="btnHRHome" Click="HRMenu_Click" Style="{StaticResource HRMenuBtn}" Height="50" Margin="0,2">
                        <TextBlock Text="🏠 Trang Chủ" Margin="15,0" FontWeight="Bold"/>
                    </ToggleButton>

                    <ToggleButton x:Name="btnHRNhanVien" Click="HRMenu_Click" Style="{StaticResource HRMenuBtn}" Height="50" Margin="0,2">
                        <TextBlock Text="⌨️ DS Nhân Viên" Margin="15,0" FontWeight="Bold"/>
                    </ToggleButton>

                    <ToggleButton x:Name="btnHRBacSi" Click="HRMenu_Click" Style="{StaticResource HRMenuBtn}" Height="50" Margin="0,2">
                        <TextBlock Text="👨‍⚕️ Danh Sách Bác Sĩ" Margin="15,0" FontWeight="Bold"/>
                    </ToggleButton>

                    <ToggleButton x:Name="btnHRKhoa" Click="HRMenu_Click" Style="{StaticResource HRMenuBtn}" Height="50" Margin="0,2">
                        <TextBlock Text="🏥 Danh Sách Khoa" Margin="15,0" FontWeight="Bold"/>
                    </ToggleButton>
                </StackPanel>

                <Button Content="ĐĂNG XUẤT" Click="HRLogOut_Click" Background="Transparent" BorderBrush="Transparent" Foreground="Red" FontWeight="Bold" Margin="15,50,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Background="#F2F6F9">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Background="#2E78A6">
                <TextBlock Text="QUẢN TRỊ NHÂN SỰ BỆNH VIỆN" Foreground="White" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" Margin="20,0"/>
            </Grid>

            <Border Grid.Row="1" Margin="15" Background="White" CornerRadius="10">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtHRWelcome" Text="Hệ thống quản lý HR" FontSize="20" Foreground="#AAA" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.RowSpan="2"/>

                    <Grid x:Name="toolbarHR" Visibility="Collapsed" Margin="0,0,0,15">
                        <Button x:Name="btnHRAddNew" Click="btnHRAddNew_Click" Content="+ Thêm Mới" Background="#2E78A6" Foreground="White" Padding="15,5" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBox x:Name="txtHRSearch" Width="180" Height="30" VerticalContentAlignment="Center"/>
                            <Button Content="🔍" Click="btnHRSearch_Click" Width="40" Background="#DDD"/>
                        </StackPanel>
                    </Grid>

                    <DataGrid x:Name="dgHRMain" Grid.Row="1" Visibility="Collapsed" AutoGenerateColumns="True" IsReadOnly="True"/>
                    <DataGrid x:Name="dgHRDept" Grid.Row="1" Visibility="Collapsed" AutoGenerateColumns="True" IsReadOnly="True"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
