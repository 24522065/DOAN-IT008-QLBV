<Window x:Class="DoAn.MainWindowPatient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hệ Thống Bệnh Nhân" Height="700" Width="1200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="4,0,0,0">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F7FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2E78A6"/>
                                <Setter Property="Foreground" Value="#2E78A6"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="10" CornerRadius="20" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.2" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#F9FBFF" CornerRadius="20,0,0,20" BorderBrush="#EEE" BorderThickness="0,0,1,0">
                <Grid>
                    <StackPanel Margin="0,30,0,0">
                        <Ellipse Width="80" Height="80" Fill="#2E78A6" Margin="0,0,0,15"/>
                        <TextBlock x:Name="txtPatientName" Text="Xin chào, Bệnh nhân" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Foreground="#2E78A6"/>
                        <TextBlock x:Name="txtPatientID" Text="ID: PAT001" HorizontalAlignment="Center" Foreground="Gray" Margin="0,5,0,30"/>

                        <Button Content="🏠   Trang Chủ" Style="{StaticResource MenuButtonStyle}" Click="Menu_Click" Tag="Home"/>
                        <Button Content="👤   Thông Tin Cá Nhân" Style="{StaticResource MenuButtonStyle}" Click="Menu_Click" Tag="Profile"/>
                        <Button Content="📋   Hồ Sơ Bệnh Án" Style="{StaticResource MenuButtonStyle}" Click="Menu_Click" Tag="History"/>
                        <Button Content="💳   Hóa Đơn Thanh Toán" Style="{StaticResource MenuButtonStyle}" Click="Menu_Click" Tag="Billing"/>
                    </StackPanel>

                    <Button Content="↪   Đăng Xuất" Style="{StaticResource MenuButtonStyle}" VerticalAlignment="Bottom" Margin="0,0,0,20" Foreground="#E53935" Click="Logout_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <TextBlock x:Name="txtTitle" Text="TRANG CHỦ" VerticalAlignment="Center" Margin="30,0" FontWeight="Bold" FontSize="18" Foreground="#444"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                            <Button Content="—" Click="Minimize_Click" Background="Transparent" BorderThickness="0" Width="40" Height="40" FontSize="16" Cursor="Hand"/>
                            <Button Content="✕" Click="Close_Click" Background="Transparent" BorderThickness="0" Width="40" Height="40" FontSize="16" Cursor="Hand" Foreground="#D32F2F"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <ContentControl x:Name="MainContainer" Grid.Row="1" Margin="20"/>
            </Grid>
        </Grid>
    </Border>
</Window>
