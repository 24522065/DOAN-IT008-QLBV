<Window x:Class="DoAn.Windows.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm nhân viên" Height="500" Width="800"
        WindowStyle="None" ResizeMode="NoResize" Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterScreen">

    <!-- Nền xám mờ bo góc -->
    <Border Background="#DCDCDC" CornerRadius="15" BorderBrush="#999" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Form -->
                <RowDefinition Height="Auto"/>
                <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <TextBlock Text="THÊM MỚI NHÂN VIÊN" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" BorderThickness="0" FontSize="18" Click="Cancel_Click"/>
            </Grid>

            <!-- Form -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <!-- Label Col 1 -->
                    <ColumnDefinition Width="*"/>
                    <!-- Input Col 1 -->
                    <ColumnDefinition Width="20"/>
                    <!-- Gap -->
                    <ColumnDefinition Width="150"/>
                    <!-- Label Col 2 -->
                    <ColumnDefinition Width="*"/>
                    <!-- Input Col 2 -->
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Right"/>
                        <Setter Property="Margin" Value="0,0,10,0"/>
                        <Setter Property="FontSize" Value="14"/>
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Height" Value="28"/>
                        <Setter Property="Margin" Value="0,6"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderBrush" Value="#888"/>
                    </Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="Height" Value="28"/>
                        <Setter Property="Margin" Value="0,6"/>
                    </Style>
                    <Style TargetType="DatePicker">
                        <Setter Property="Height" Value="28"/>
                        <Setter Property="Margin" Value="0,6"/>
                    </Style>
                </Grid.Resources>

                <!-- Cột trái -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Nhập họ tên:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtName"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nhập CCCD:"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtCID"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Chọn giới tính:"/>
                <ComboBox Grid.Row="2" Grid.Column="1" x:Name="cbGender" SelectedIndex="0">
                    <ComboBoxItem Content="Male"/>
                    <ComboBoxItem Content="Female"/>
                </ComboBox>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Số điện thoại:"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtPhone"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Nhập địa chỉ:"/>
                <TextBox Grid.Row="4" Grid.Column="1" x:Name="txtAddress"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Email:"/>
                <TextBox Grid.Row="5" Grid.Column="1" x:Name="txtEmail"/>

                <!-- Cột phải -->
                <TextBlock Grid.Row="0" Grid.Column="3" Text="Ngày sinh:"/>
                <DatePicker Grid.Row="0" Grid.Column="4" x:Name="dpDOB"/>

                <TextBlock Grid.Row="1" Grid.Column="3" Text="Ngày vào làm:"/>
                <DatePicker Grid.Row="1" Grid.Column="4" x:Name="dpStartDate"/>

                <TextBlock Grid.Row="2" Grid.Column="3" Text="Mức lương:"/>
                <TextBox Grid.Row="2" Grid.Column="4" x:Name="txtSalary"/>

                <TextBlock Grid.Row="3" Grid.Column="3" Text="Chọn Khoa (Dept):"/>
                <ComboBox Grid.Row="3" Grid.Column="4" x:Name="cbDept" 
          DisplayMemberPath="Depart_Name" SelectedValuePath="Depart_ID"/>

                <TextBlock Grid.Row="4" Grid.Column="3" Text="Chức vụ (Type):"/>
                <ComboBox Grid.Row="4" Grid.Column="4" x:Name="cbType">
                    <!-- Gán Tag để lấy đúng mã EC003/DC002 -->
                    <ComboBoxItem Content="Nhân viên nhập liệu" Tag="EC003"/>
                    <ComboBoxItem Content="Bác sĩ" Tag="DC002"/>
                </ComboBox>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="TẠO HỒ SƠ" Click="Save_Click" Width="120" Height="35" Margin="10" 
                        Background="#FFF" FontWeight="Bold" BorderBrush="#888"/>
                <Button Content="THOÁT" Click="Cancel_Click" Width="100" Height="35" Margin="10" 
                        Background="#FFF" BorderBrush="#888"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
