<Window x:Class="DoAn.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QUẢN LÝ NHÂN SỰ"
        WindowStyle="None" ResizeMode="NoResize"
        Height="650" Width="1100">

    <Window.Resources>
        <!-- Style Menu Button -->
        <Style x:Key="MenuBtn" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Background="White" BorderBrush="#DDD" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <!-- Avatar -->
                <StackPanel HorizontalAlignment="Center" Margin="0,20,0,30">
                    <Ellipse Width="70" Height="70">
                        <Ellipse.Fill>
                            <SolidColorBrush Color="#DDD"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="Admin User" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- MENU ITEMS -->
                <StackPanel>
                    <ToggleButton x:Name="btnHome" Click="Menu_Click" Style="{StaticResource MenuBtn}" Height="50" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" Margin="10,0,15,0" FontSize="18"/>
                            <TextBlock Text="Trang Chủ" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                        </StackPanel>
                    </ToggleButton>

                    <ToggleButton x:Name="btnNhanVien" Click="Menu_Click" Style="{StaticResource MenuBtn}" Height="50" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⌨️" Margin="10,0,15,0" FontSize="18"/>
                            <TextBlock Text="DS Nhân viên nhập liệu" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                        </StackPanel>
                    </ToggleButton>

                    <ToggleButton x:Name="btnBacSi" Click="Menu_Click" Style="{StaticResource MenuBtn}" Height="50" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👨‍⚕️" Margin="10,0,15,0" FontSize="18"/>
                            <TextBlock Text="Danh sách bác sĩ" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                        </StackPanel>
                    </ToggleButton>

                    <!-- THÊM NÚT DANH SÁCH KHOA -->
                    <ToggleButton x:Name="btnKhoa" Click="Menu_Click" Style="{StaticResource MenuBtn}" Height="50" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏥" Margin="10,0,15,0" FontSize="18"/>
                            <TextBlock Text="Danh sách khoa" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>

                <Button x:Name="btnLogOut" Content="ĐĂNG XUẤT" Click="btnLogOut_Click"
                        Background="Transparent" BorderBrush="Transparent" FontWeight="Bold"
                        HorizontalAlignment="Left" Margin="15,50,0,0" Foreground="Red"/>
            </StackPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1" Background="#F2F6F9">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Grid Grid.Row="0" Background="#2E78A6">
                <TextBlock Text="HỆ THỐNG QUẢN LÝ BỆNH VIỆN" Foreground="White" FontWeight="Bold" FontSize="20" 
                           VerticalAlignment="Center" Margin="25,0,0,0"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <Button Content="—" Width="40" Background="Transparent" Foreground="White" BorderThickness="0" Click="Min_Click" FontSize="16"/>
                    <Button Content="✕" Width="40" Background="Transparent" Foreground="White" BorderThickness="0" Click="Close_Click" FontSize="16"/>
                </StackPanel>
            </Grid>

            <!-- Workspace -->
            <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="10">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtHomeMessage" Text="Chào mừng Admin!" 
                               FontSize="24" Foreground="#888" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.RowSpan="2"/>

                    <!-- TOOLBAR (Thêm & Tìm kiếm) -->
                    <Grid x:Name="toolbarNhanSu" Visibility="Collapsed" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="btnAddEmployee" Click="btnAddEmployee_Click"
                                Content="+ Thêm mới" Background="#2E78A6" Foreground="White"
                                Padding="20,10" FontWeight="Bold" BorderThickness="0"/>

                        <Border Grid.Column="2" BorderBrush="#CCC" BorderThickness="1" CornerRadius="20" Padding="10,0" Background="White">
                            <StackPanel Orientation="Horizontal">
                                <TextBox x:Name="txtSearch" Width="200" Height="35" VerticalContentAlignment="Center" 
                                         BorderThickness="0" Margin="5,0" Tag="Tìm kiếm..."/>
                                <Button x:Name="btnSearch" Click="btnSearch_Click" Content="🔍" 
                                        Background="Transparent" BorderThickness="0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- DATA GRID CHO NHÂN VIÊN -->
                    <DataGrid x:Name="dgData" Grid.Row="1" AutoGenerateColumns="False" Visibility="Collapsed"
                              CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" 
                              GridLinesVisibility="Horizontal" RowHeight="35" Background="White">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã NV" Binding="{Binding Emp_ID}" Width="90" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Họ tên" Binding="{Binding Emp_Name}" Width="160"/>
                            <DataGridTextColumn Header="Giới tính" Binding="{Binding Gender}" Width="80"/>
                            <DataGridTextColumn Header="Ngày sinh" Binding="{Binding Day_Of_Birth, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="100"/>
                            <DataGridTextColumn Header="Loại" Binding="{Binding Emp_Type}" Width="80"/>
                            <DataGridTextColumn Header="Khoa" Binding="{Binding Depart_ID}" Width="80"/>
                            <DataGridTextColumn Header="Lương" Binding="{Binding Salary, StringFormat={}{0:N0}}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- DATA GRID CHO KHOA (Mới) -->
                    <DataGrid x:Name="dgDept" Grid.Row="1" AutoGenerateColumns="False" Visibility="Collapsed"
                              CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" 
                              GridLinesVisibility="Horizontal" RowHeight="35" Background="White">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Khoa" Binding="{Binding Depart_ID}" Width="120" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Tên Khoa" Binding="{Binding Depart_Name}" Width="*"/>
                            <DataGridTextColumn Header="Trưởng Khoa" Binding="{Binding Head_Of_Depart}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
